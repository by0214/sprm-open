# ProcessBench Data

Place ProcessBench files under `raw/` or pass their location with `--data-dir` when running:

```bash
python -m sprm.eval.processbench --data-dir data/processbench/raw ...
```

The benchmark data itself should not be committed.

# PRMBench Data

The default release path should load PRMBench with Hugging Face datasets, for example:

```bash
python -m sprm.eval.prmbench --dataset-name hitsmy/PRMBench_Preview ...
```

If a local mirror is used, pass its path explicitly. Do not commit benchmark data.

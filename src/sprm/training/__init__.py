"""Training entrypoints for the three SPRM stages.

Import concrete APIs from their stage modules, for example:
`sprm.training.stage1_value_model`, `sprm.training.stage2_credit_labels`, and
`sprm.training.stage3_dual_prm`.
"""
